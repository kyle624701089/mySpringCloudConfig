spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kyle624701089/springCloudConfig
          search-paths: respo
          username:
          password:
      label: master
server:
  port: 8090